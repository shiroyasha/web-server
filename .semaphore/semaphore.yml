version: v1.0
name: "Web Server Pipeline"
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Test"
    task:
      jobs:
      - name: "Web Server Tests"
        commands:
          - checkout
          - ls -1
          - sleep 200
